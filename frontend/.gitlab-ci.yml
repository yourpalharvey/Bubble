image: node:latest

stages:
  - test
  - deploy

before_script:
  - cd frontend
  - yarn install --cache-folder .yarn

    
test:
  stage: test
  script:
    - yarn -v
    - yarn test

deploy:
  stage: deploy
  script:
    - echo -e "Deploying frontend"
  only:
    - main